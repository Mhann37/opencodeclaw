# March 19, 2026 — Daily Log

## OpenClaw Configuration & Web Search
**COMPLETED:**
- Enabled Brave Search API via `openclaw configure --section web`
- Brave API key added: `BSAsrNw7IkAE7pNhWxN_AdVNI5qkByT`
- `web_fetch` (keyless HTTP) enabled
- Gateway restarted successfully
- **Web search is now fully operational** ✅

## Agent-browser Installation & Testing
**COMPLETED:**
- Installed `agent-browser` npm package globally
- Issue: Downloaded Windows binary (x64) instead of Linux ARM64
- **Solution:** Use system Chromium instead (`sudo apt install chromium`)
- Tested browser automation:
  - Google search → blocked by CAPTCHA (expected for bots)
  - Bing search → works perfectly, no CAPTCHA
  - DuckDuckGo → also works without CAPTCHA walls
- **Current capability:** Browser automation works for Bing/DDG; Google requires different approach

## Calendar App Deployment (Personal Projects)
**GitHub Push:**
- Pushed `~/personal-calendar-app` to GitHub
- Repo: `https://github.com/Mhann37/personal-calendar-app`
- Deleted duplicate `App.js` (was conflicting with `App.jsx`)

**Vercel Deployment Issues & Fixes:**
1. **Build failure #1:** Duplicate `App.js` causing React confusion
   - Fix: Deleted stale boilerplate files
   - Redeployed ✅

2. **Build failure #2-5:** ESLint errors (unused variables treated as errors in CI)
   - React Scripts build fails when `process.env.CI = true`
   - Vercel sets CI=true automatically
   - Variables: `task` (App.jsx:42), `data` (App.jsx:140), `file` (QuickAdd.jsx:63), `getTaskTextColor` (WeekView.jsx:25)
   - Initial fixes used sed/comments → syntax errors
   - Final fix: Properly removed unused variable assignments
   - Last commit: `107489f` (git reset to clean state, then proper edits)
   - Status: Awaiting Vercel rebuild (as of 17:35 AEDT)

**Code Changes:**
- `calendar.addTask(...)` — removed unused `const task =` assignment
- `JSON.parse(...)` — removed unused `const data =` assignment
- Removed unused `file` variable capture in QuickAdd
- Removed entirely unused `getTaskTextColor` function in WeekView

## Key Learnings
- **Google search + bot automation:** Blocked by CAPTCHA; not viable for automated search
- **Brave API vs agent-browser:** Complementary tools
  - Brave API: Fast, structured results, no CAPTCHA, good for programmatic use
  - agent-browser: Visual results, form automation, works on Bing/DDG
- **Vercel CI mode:** Treats warnings as errors; need clean linting for production builds
- **Git workflows:** force-with-lease is safer than force for remote conflicts

## Health Context
- **Medication**: Mounjaro (tirzepatide) 0.3mg weekly, considering increase to 0.5mg on return
- **Weight trajectory**: 127.8kg (27 Jan) → 118.9kg (20 Mar) = 9kg in ~8 weeks (~1.1kg/week)
- **Sleep quality**: Variable 51-87% performance; consistency drifting on disrupted schedule
- **Recovery**: 57% today (low); HRV 41ms, RHR 50bpm (solid)
- **Training**: Rest days Sat-Sun (planned deload); may lift Sunday evening on return

## Status Summary
- **OpenClaw:** Restored to full working state ✅ (Brave + web_fetch + browser automation)
- **Calendar app:** Awaiting final Vercel build (code is clean, should deploy on next push)
- **Fraud playbook:** Still in draft, awaiting Matt's content review and PDF formatting
- **Next steps:** Check Vercel deployment result; if successful, calendar app is live

## Later Session (Evening) — Additional Work

**Calendar App Mobile Optimization:**
- Opus redesigned for mobile: AgendaView component + responsive layout
- Created branch `001-mobile-optimization`
- Fixed unused imports
- Deployed to Vercel (stable)

**WineNight Blind Tasting App:**
- Cloned from GitHub (Mhann37/WineNight)
- Opus completed `001-production-hardening`:
  - CSV export, spectator/TV mode, score histogram, tasting notes
  - Production stability (storage wrapper, validation, error handling)
  - Participant readiness checks
  - Pass-the-phone model (single device, guest taps name to score)
- Opus completed `002-join-session-flow`:
  - PIN-based join (Kahoot-style)
  - Guests enter 4-digit PIN + name to join session
- **Critical issue discovered:** localStorage is device-specific
  - When guest joins on different device, their localStorage is empty
  - "No tasting found" error
  - **Requires backend** (Firebase/Supabase/Vercel KV) for real-time sync
- **Status:** Paused/killed (Matt made other arrangements for wine night)

**Decisions Made:**
- Kill WineNight project (real-time sync would require backend; Matt has alternatives)
- Pause fraud playbook until next week
- Resume OpenClaw focus (stable)

## Token Usage
- Session started ~265k cumulative
- Additional Opus work: ~30k tokens
- Pre-compaction at ~365k
- Embedding quota still exhausted (will need to be resolved separately)
