- SetlistArt backlog after Package B deployment review (screenshots on 2026-03-17):
  - Biggest remaining UX blemish: concert results still show `Unknown city`, which hurts trust and makes result selection feel rough.
  - Improve concert result presentation/readability so the chooser feels more polished and confidence-building.
  - Improve result ranking/confidence so likely matches feel more obviously helpful.
  - Reduce backend/API vibes in wording where possible (e.g. less emphasis on `setlist.fm` as the user-facing action, more outcome-oriented language).
  - Overall direction: keep the current Package B improvements live; next likely focus is result quality/presentation and matching confidence rather than broad new UI work.
- Reliability lesson (cron + WHOOP) on 2026-03-17: do not assume a scheduled job is correctly targeted just because it exists. The first 7:30am morning-health-brief failed because cron delivery used `channel=last`/announce without an explicit Telegram target chat id. Fix: set explicit Telegram `to=8593559681` for recurring delivery jobs.
- Reliability lesson (WHOOP): successful auth / token refresh does not guarantee data endpoint access. On 2026-03-17, WHOOP token refresh succeeded under the `openclaw` user, but data commands still failed because direct access to `https://api.prod.whoop.com/developer/v2/...` returned Cloudflare 403/1010 access denied from this runtime/IP path. Future diagnostics should separately test: auth status, token refresh, and an actual data endpoint.
- Morning brief cron hardening: configure the 7:30am brief to fall back to latest known WHOOP context + latest StrengthInsight extract if live WHOOP fetch fails, and enable failure alerts instead of silent misses.
- SetlistArt artist-suggestion pivot to curated top-artist-first matching produced a dramatic improvement in live behavior on 2026-03-17.
- New backlog item for SetlistArt: expand the curated top-artist list beyond ~200 to improve coverage while keeping the curated-first/fallback model.
- Current product judgment: curated mainstream-first artist suggestions + raw fallback is a much better 80/20 solution than trying to rank noisy raw setlist.fm matches directly.
- Health/training context from Matt: dealing with elbow tendinitis. It has improved, but was likely triggered a few weeks ago by heavy lat pulldowns.
- Future health/training guidance should take this into account and emphasize balancing push vs pull exercises to avoid imbalance and aggravation.
- OpenClaw maintenance incident (2026-03-17): Matt tried switching the gateway to a Claude model, which broke the dashboard and Telegram until the gateway service/permissions are reconfigured. He kept a full backup (`openclaw-backup-20260317-202755.tar.gz`) of `~/.openclaw/` to fall back on if needed.
